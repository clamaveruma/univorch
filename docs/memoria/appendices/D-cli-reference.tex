% Apéndice D - Referencia del cliente CLI.
% Fuente: el propio binario --help; las tablas se pueden generar a
% partir de los argparse declarados en src/univorch/interfaces/cli/app.py.

\umaappendix{Referencia del cliente CLI}
\label{ap:cli-reference}

% TODO: redactar.
% Tabla de comandos con sintaxis, descripción y ejemplo.
% Comandos a documentar:
%   load, tree, ls, list, cd, pwd, inspect, deploy, undeploy, start,
%   stop, status, connect, help, quit.
% Y los argumentos del CLI:
%   --remote URL, UNIVORCH_REMOTE env var, default localhost:8080.

Pendiente de redactar. Mientras tanto, la ayuda de cada orden está
disponible mediante \texttt{help <orden>} dentro del REPL.
