"""Command-line interface built with cmd2.

Supports two modes:
  - Bash mode: single commands (univorch deploy /path) — scriptable.
  - REPL mode: interactive shell (./univorch.sh cli) — for exploration.
"""
