% Chapter 3 - Methodology.
% Sources: docs/plan.md, docs/environment.md, claude/desarrollo.md,
% claude/diario.md, .github/workflows/*.yml.
% Target length: 6-8 pages.

\chapter{Methodology}
\label{ch:metodologia}

% TODO: chapter intro, ~half a page.
% "The project follows a phased process with iterative sprints, a
% strict code-quality discipline, and an AI-assisted collaboration
% model. This chapter describes the process framework, the
% reproducible development environment, the continuous quality
% practices, and the most relevant methodological choices for both
% the development and the future maintainability of the system."

\section{Process framework: phases and sprints}
\label{sec:proceso}

% TODO: draft. Source: docs/plan.md.
% - Seven phases: problem definition, requirements, architecture,
%   technologies, environment, iterative development, evaluation and
%   report.
% - Each phase produces a formal deliverable (vision.md,
%   requirements.md, architecture.md, technologies.md,
%   environment.md, code).
% - Inside iterative development, sprints with agreed scope and a
%   formal close in the journal.
% - Journal and decisions log as the traceability mechanism.

\section{Reproducible development environment}
\label{sec:entorno}

% TODO: draft. Source: docs/environment.md.
% - Shared devcontainer across machines and platforms (VSCode Dev
%   Containers, Remote-SSH, Codespaces).
% - The same devcontainer.json brings up local, remote server and
%   cloud; the resulting environment is bit-for-bit equivalent.
% - uv as the dependency and virtual-environment manager (Rust
%   binary, single configuration in pyproject.toml).
% - Development image (Microsoft Python + features) is different from
%   the production image (multi-stage minimal).
% - recterm tool so the AI-assisted session can observe the
%   developer's terminal during debugging.

\section{Quality discipline}
\label{sec:calidad}

% TODO: draft. Source: docs/technologies.md sections 2 and 5, plus the
% workflows in .github/workflows/.
% - TDD from the first commit of the code base.
% - Ruff as linter + formatter (replaces flake8 + isort + black).
% - mypy in strict mode over src/univorch/.
% - pytest + pytest-cov + Hypothesis (property-based testing for the
%   resolver, which is a pure function).
% - Target and achieved coverage at the end of the PoC (>=95% in the
%   domain logic; 244 tests passing at the close of Sprint 3).
% - Table of tests by type (unit, integration, property) and module.

\section{Continuous integration and delivery}
\label{sec:cicd}

% TODO: draft. Source: .github/workflows/ci.yml and publish.yml.
% - CI on every push and PR to main: ruff + format + mypy + pytest.
% - Publishing to ghcr.io triggered by semver tags: explicit user
%   control over when a new version exists. Tags vX.Y.Z (immutable)
%   and :latest (mutable). Versioning policy: 0.x while the API is
%   unstable.
% - Public image in the GitHub Container Registry, downloadable
%   without login. The lecturer only needs Docker.

\section{AI-assisted development}
\label{sec:ia}

% TODO: draft. This section is original and worth defending.
% - Assistant: Claude (Anthropic). Opus or Sonnet model depending on
%   the phase (critical decisions vs iterative work).
% - Persistent-context mechanism: claude/ files versioned in the
%   repository. CLAUDE.md as the entry point with imports.
% - Journal (chronological), decisions log (DEC-001 to DEC-036),
%   pending-ideas bank, development context: separation of concerns
%   so any new session can recover the full context.
% - Working pattern: explain -> clarify -> confirm -> execute. The
%   student keeps control and understands each decision.
% - Boundary: the assistant writes the code, the student takes the
%   decisions, and the responsibility for defending the report is
%   the student's.

\section{Traceability and knowledge management}
\label{sec:trazabilidad}

% TODO: draft. Source: claude/decisiones.md, claude/diario.md,
% claude/ideas-pendientes.md.
% - Chronological journal with every decision taken in its temporal
%   context.
% - Numbered technical-decisions log (DEC-001 to DEC-036) with mutual
%   traceability and journal references.
% - Pending-ideas bank, organised by area: near-term, web GUI,
%   teaching layer, TFG report, UX...
% - Controlled vocabulary in docs/glossary.md, single source of
%   truth for the report's glossary and for the future administrator
%   manual.
