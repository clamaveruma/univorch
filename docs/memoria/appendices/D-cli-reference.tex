% Appendix D - CLI reference.
% Source: the binary itself via --help; tables can be generated from
% the argparse definitions in src/univorch/interfaces/cli/app.py.

\umaappendix{CLI reference}
\label{ap:cli-reference}

% TODO: draft.
% Command table with syntax, description and an example.
% Commands to document:
%   load, tree, ls, list, cd, pwd, inspect, deploy, undeploy, start,
%   stop, status, connect, help, quit.
% CLI arguments to document:
%   --remote URL, UNIVORCH_REMOTE env var, default localhost:8080.

To be drafted. Until then, each command's help is available with
\texttt{help <command>} inside the REPL.
