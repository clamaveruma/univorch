# yaml-language-server: $schema=../docs/schema/definition.schema.json
#
# UnivOrch demo — Sprint 1
# Relative definition: load with `load demo/setup.yml [/destination]`.
# The destination is the parent folder; defaults to the current folder (root).
# Folders are keys ending in '/'; machines are keys without '/'.

kind: definition
version: "1"

lab/:
  description: "Computer Science Lab"
  networks/:
    description: "Computer Networks — Fall 2026"
    student01:
      description: "Student 01 workstation"
      hypervisor: mock
      base_vm: linux-base
      cpu: 2
      memory_mb: 2048
      disk_gb: 20
    student02:
      hypervisor: mock
      base_vm: linux-base
      cpu: 2
    student03:
      hypervisor: mock
      base_vm: linux-base
      cpu: 2
