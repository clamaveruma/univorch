% Chapter 4 - Development, testing and results.
% Largest chapter. Sources: docs/requirements.md, docs/architecture.md,
% docs/diagrams.md, claude/decisiones.md (DEC-001 to DEC-036), code
% under src/, tests under tests/.
% Target length: 18-22 pages.

\chapter{Development, testing and results}
\label{ch:desarrollo}

% TODO: chapter intro, ~half a page.
% "This chapter describes the full PoC development cycle, organised
% in six blocks: requirements analysis, architectural design,
% implementation, testing strategy, functional demonstration and
% qualitative comparison with the supervisor's previous libraries.
% The detailed requirements analysis is moved to Appendix
% \ref{ap:requirements}; here we present the structured summary."

\section{Requirements analysis}
\label{sec:analisis}

% TODO: draft. Summary of docs/requirements.md (full version in
% Appendix B). Structure:
% 4.1.1 Actors and main use cases
%   - Actor table covering the orchestrator core and the teaching
%     layer roles.
%   - Most representative use cases of core and teaching layer (not
%     all of them).
% 4.1.2 Functional requirements (grouped by topic, not as an
%   exhaustive list).
% 4.1.3 Non-functional requirements (table).
% 4.1.4 Constraints and out of scope.
%
% Standard text: "The full analysis is developed in Appendix
% \ref{ap:requirements}; this section presents the structuring
% elements."

\section{Architectural design}
\label{sec:diseno}

% TODO: draft. Source: docs/architecture.md + docs/diagrams.md.
% Important: refresh both documents before writing this section
% (they still miss Sprint 2 Piece 3 and the whole Sprint 3).
%
% Structure:
% 4.2.1 Two-layer architecture (DEC-004)
%   - C4 Context diagram (figure).
%   - C4 Container diagram (figure).
% 4.2.2 Domain model
%   - Descriptor tree with materialized path (DEC-008).
%   - Cascade inheritance by data type (DEC-026).
%   - Template lexical closure (refinement of DEC-012, 2026-06-06).
% 4.2.3 Patterns applied
%   - Facade (OrchestratorService, DEC-031).
%   - Command pattern (DEC-028).
%   - Repository (DEC-007/030).
%   - Strategy / ABC for connectors (DEC-029).
%   - Protocol for the CLI / HTTP client boundary.
% 4.2.4 REST as the public boundary
%   - Defensible argument: the REST API does not exist for remote
%     CLI convenience (the admin has SSH); it exists for external
%     integrations.
%   - CLI dual-mode in-process / HTTP was discarded in favour of the
%     pure-client model (Sprint 3.2 B-2), to align with
%     docker / kubectl.
% 4.2.5 Component diagram (figure) and deployment diagram (figure).

\section{Implementation}
\label{sec:implementacion}

% TODO: draft. Structure by layer, not by sprint (sprints matter for
% the methodology chapter, not for presenting the product).
% 4.3.1 Orchestrator core
%   - Pydantic models.
%   - Pure resolver (function + Hypothesis tests).
%   - Jobs engine and Commands.
%   - TinyDB repositories.
%   - Facade service.
% 4.3.2 Mock connector
%   - In-memory state, demo templates pre-loaded.
%   - No external dependencies: the whole system runs without a real
%     hypervisor.
% 4.3.3 CLI interface
%   - cmd2 with argparse, dual REPL / bash mode.
%   - HTTP client injected, translates transport errors into
%     OperationError with actionable messages.
% 4.3.4 REST daemon
%   - FastAPI + uvicorn, paths as query parameters.
%   - Healthcheck, error coding 200/400/422.
% 4.3.5 Packaging and distribution
%   - Multi-stage Dockerfile, docker-compose with a named volume.
%   - install.sh installer with Docker, port and Compose detection.
%   - Image published in ghcr.io with semver tags.

\section{Testing strategy}
\label{sec:pruebas}

% TODO: draft.
% 4.4.1 Test pyramid applied.
% 4.4.2 Unit tests by module, with high coverage in domain logic.
% 4.4.3 Integration tests with in-memory TinyDB and the real mock
%   connector.
% 4.4.4 Property-based tests with Hypothesis: the resolver, a pure
%   function, is the ideal case. Three properties checked.
% 4.4.5 REST tests with FastAPI TestClient as an httpx client that
%   routes to the ASGI app in-process. Catches client-server
%   divergences without network.
% 4.4.6 Final metrics: 244 tests passing at the close of Sprint 3,
%   95-100% in central modules.

\section{Functional demonstration}
\label{sec:demo}

% TODO: draft. Screenshots to include (figures/ folder).
% - Tree structure loaded from the demo.
% - inspect showing inheritance and closure in action.
% - Full lifecycle deploy/start/status/stop/undeploy.
% - HTTP health check via curl and the browser (/docs).
% - Walk through the API in Swagger UI.

\section{Comparison with esxobjects and yamlinfr}
\label{sec:comparativa}

% TODO: draft.
% - Comparison table: scope, dependencies, descriptor model,
%   extensibility, test coverage, distribution.
% - Qualitative read: yamlinfr / esxobjects solved their case, bound
%   to VMware. UnivOrch generalises and opens the door to other
%   hypervisors without touching the core.
% - Honest discussion: UnivOrch is not yet a drop-in operational
%   replacement (real connector is still pending); it is a proof of
%   concept of the model.
