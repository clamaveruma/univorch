% Chapter 4 - Development, testing and results.
% Largest chapter. Sources: docs/requirements.md, docs/architecture.md,
% docs/diagrams.md, claude/decisiones.md (DEC-001 to DEC-036), code
% under src/, tests under tests/.
% Target length: 18-22 pages.

\chapter{Development, testing and results}
\label{ch:desarrollo}

% TODO: chapter intro, ~half a page.
% "This chapter describes the full PoC development cycle, organised
% in six blocks: requirements analysis, architectural design,
% implementation, testing strategy, functional demonstration and
% qualitative comparison with the supervisor's previous libraries.
% The detailed requirements analysis is moved to Appendix
% \ref{ap:requirements}; here we present the structured summary."

\section{Requirements analysis}
\label{sec:analisis}

% TODO: draft. Summary of docs/requirements.md (full version in
% Appendix B). Structure:
% 4.1.1 Actors and main use cases
%   - Actor table covering the orchestrator core and the teaching
%     layer roles.
%   - Most representative use cases of core and teaching layer (not
%     all of them).
% 4.1.2 Functional requirements (grouped by topic, not as an
%   exhaustive list).
% 4.1.3 Non-functional requirements (table).
% 4.1.4 Constraints and out of scope.
%
% Standard text: "The full analysis is developed in Appendix
% \ref{ap:requirements}; this section presents the structuring
% elements."

\section{Architectural design}
\label{sec:diseno}

This section is currently a \emph{provisional figure gallery}. The full
text is pending; the diagrams below are the up-to-date sources from
\texttt{docs/architecture.md} and \texttt{docs/diagrams.md} and will
anchor the final prose. Each subsection follows the planned outline of
the chapter so that the eventual narrative can fill in around the
already-rendered figures.

\subsection{Two-layer architecture (DEC-004)}
\label{subsec:two-layers}

% TODO: prose. Generic orchestrator core (layer 1) + thin teaching
% application (layer 2). Same engine reusable for CTF, research labs,
% corporate training.

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.95\linewidth]{two-layers.pdf}
  \caption{Two-layer overview: the daemon \texttt{univorchd} hosts the
    core; every client (CLI, web GUI, teaching app) speaks REST to it.}
  \label{fig:two-layers}
\end{figure}

\subsection{C4 context and containers}
\label{subsec:c4-l1-l2}

% TODO: prose. C4 model levels 1 and 2 of the system, who uses what.

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.95\linewidth]{context.pdf}
  \caption{C4 level 1 (Context): people and external systems around
    UnivOrch.}
  \label{fig:c4-context}
\end{figure}

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.95\linewidth]{containers.pdf}
  \caption{C4 level 2 (Containers): independently runnable units. The
    daemon \texttt{univorchd} and the CLI \texttt{univorch} are two
    separate binaries; every client speaks HTTP.}
  \label{fig:c4-containers}
\end{figure}

\subsection{Deployment view}
\label{subsec:deployment}

% TODO: prose. Real Sprint 3 topology: one Linux host with Docker,
% the daemon in a container, the TinyDB file on a named volume.

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.95\linewidth]{deployment.pdf}
  \caption{Deployment topology since Sprint 3: clients on tier 1, the
    daemon container plus its named volume on tier 2, and the
    hypervisor hosts on tier 3.}
  \label{fig:deployment}
\end{figure}

\subsection{Components (as-built)}
\label{subsec:components}

% TODO: prose. Modules inside the daemon. Status legend: solid =
% implemented, dashed = designed but pending (web GUI, real
% connectors).

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.95\linewidth]{components.pdf}
  \caption{C4 level 3 (Components): modules inside \texttt{univorchd}
    after Sprint 3. Solid green = implemented; dashed grey = designed,
    not yet implemented.}
  \label{fig:components}
\end{figure}

\subsection{Domain model}
\label{subsec:domain-model}

% TODO: prose. Descriptor tree with materialized path (DEC-008);
% cascade inheritance by data type (DEC-026); template lexical closure
% (refinement of DEC-012, 2026-06-06).

\begin{sidewaysfigure}
  \centering
  \includegraphics[width=0.95\linewidth]{domain-models.pdf}
  \caption{Domain model and connector contract (C4 code level, view
    1/2). Persisted entities (\texttt{Folder}, \texttt{Descriptor},
    \texttt{Job}), input documents (\texttt{DefinitionDocument} and
    friends) and the abstract \texttt{HypervisorConnector}.}
  \label{fig:domain-models}
\end{sidewaysfigure}

\subsection{Patterns applied}
\label{subsec:patterns}

% TODO: prose. Facade (OrchestratorService, DEC-031), Command pattern
% (DEC-028), Repository (DEC-007/030), Strategy / ABC for connectors
% (DEC-029), Protocol for the CLI / HTTP client boundary.

\begin{sidewaysfigure}
  \centering
  \includegraphics[width=0.95\linewidth]{engine-rest.pdf}
  \caption{Engine, service and REST boundary (C4 code level, view
    2/2). The \texttt{OrchestratorAPI} Protocol unifies the in-process
    service and the HTTP client; clients depend on the Protocol, not
    on either concrete class.}
  \label{fig:engine-rest}
\end{sidewaysfigure}

\subsection{Declarative load flow (DEC-027)}
\label{subsec:load-flow}

% TODO: prose. Parse, diff, validate (fail-fast), plan/dry-run,
% execute. Plan is the same flow with execute disabled. Validation
% lives in the service, never creates a Job until accepted.

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.75\linewidth]{load-flow.pdf}
  \caption{Stages of \texttt{load} / \texttt{plan}: validation
    rejects \emph{before} any state change.}
  \label{fig:load-flow}
\end{figure}

\subsection{Descriptor state machine (DEC-022, DEC-032)}
\label{subsec:state-machine}

% TODO: prose. Four descriptor states; runtime states are reported by
% the hypervisor and not stored. State changes only as the result of a
% Job. broken is the precise definition added in DEC-032.

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.85\linewidth]{state-machine.pdf}
  \caption{Descriptor state machine. Runtime states (running,
    stopped, paused) belong to the hypervisor and are not persisted by
    the orchestrator.}
  \label{fig:state-machine}
\end{figure}

\subsection{REST as the public boundary}
\label{subsec:rest-boundary}

% TODO: prose. Defensible argument: the REST API does not exist for
% remote CLI convenience (the admin has SSH); it exists for external
% integrations. CLI dual-mode in-process / HTTP was discarded in
% favour of the pure-client model (Sprint 3.2 B-2), to align with
% docker / kubectl.

\section{Implementation}
\label{sec:implementacion}

% TODO: draft. Structure by layer, not by sprint (sprints matter for
% the methodology chapter, not for presenting the product).
% 4.3.1 Orchestrator core
%   - Pydantic models.
%   - Pure resolver (function + Hypothesis tests).
%   - Jobs engine and Commands.
%   - TinyDB repositories.
%   - Facade service.
% 4.3.2 Mock connector
%   - In-memory state, demo templates pre-loaded.
%   - No external dependencies: the whole system runs without a real
%     hypervisor.
% 4.3.3 CLI interface
%   - cmd2 with argparse, dual REPL / bash mode.
%   - HTTP client injected, translates transport errors into
%     OperationError with actionable messages.
% 4.3.4 REST daemon
%   - FastAPI + uvicorn, paths as query parameters.
%   - Healthcheck, error coding 200/400/422.
% 4.3.5 Packaging and distribution
%   - Multi-stage Dockerfile, docker-compose with a named volume.
%   - install.sh installer with Docker, port and Compose detection.
%   - Image published in ghcr.io with semver tags.

\section{Testing strategy}
\label{sec:pruebas}

% TODO: draft.
% 4.4.1 Test pyramid applied.
% 4.4.2 Unit tests by module, with high coverage in domain logic.
% 4.4.3 Integration tests with in-memory TinyDB and the real mock
%   connector.
% 4.4.4 Property-based tests with Hypothesis: the resolver, a pure
%   function, is the ideal case. Three properties checked.
% 4.4.5 REST tests with FastAPI TestClient as an httpx client that
%   routes to the ASGI app in-process. Catches client-server
%   divergences without network.
% 4.4.6 Final metrics: 244 tests passing at the close of Sprint 3,
%   95-100% in central modules.

\section{Functional demonstration}
\label{sec:demo}

% TODO: draft. Screenshots to include (figures/ folder).
% - Tree structure loaded from the demo.
% - inspect showing inheritance and closure in action.
% - Full lifecycle deploy/start/status/stop/undeploy.
% - HTTP health check via curl and the browser (/docs).
% - Walk through the API in Swagger UI.

\section{Comparison with esxobjects and yamlinfr}
\label{sec:comparativa}

% TODO: draft.
% - Comparison table: scope, dependencies, descriptor model,
%   extensibility, test coverage, distribution.
% - Qualitative read: yamlinfr / esxobjects solved their case, bound
%   to VMware. UnivOrch generalises and opens the door to other
%   hypervisors without touching the core.
% - Honest discussion: UnivOrch is not yet a drop-in operational
%   replacement (real connector is still pending); it is a proof of
%   concept of the model.
