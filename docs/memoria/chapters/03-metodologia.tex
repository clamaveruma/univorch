% Capítulo 3 - Metodología de trabajo.
% Fuentes: docs/plan.md, docs/environment.md, claude/desarrollo.md,
% claude/diario.md, los workflows .github/workflows/*.yml.
% Extensión objetivo: 6-8 páginas.

\chapter{Metodología de trabajo}
\label{ch:metodologia}

% TODO: introducción del capítulo, ~media página.
% "El proyecto se ha desarrollado siguiendo un proceso por fases con
% sprints iterativos, una disciplina estricta de calidad de código y un
% modelo de colaboración con asistencia de IA. Este capítulo describe
% el marco de proceso, el entorno reproducible, las herramientas de
% calidad continua y las decisiones de método más relevantes para el
% desarrollo y para la futura mantenibilidad."

\section{Marco de proceso: fases y sprints}
\label{sec:proceso}

% TODO: redactar. Insumo: docs/plan.md.
% - 7 fases: definición del problema, requisitos, arquitectura,
%   tecnologías, entorno, desarrollo iterativo, evaluación y memoria.
% - Cada fase con su entregable formal (vision.md, requirements.md,
%   architecture.md, technologies.md, environment.md, código).
% - Dentro del desarrollo iterativo, sprints con scope acordado y
%   cierre formal en el diario.
% - Diario y banco de decisiones como mecanismos de trazabilidad.

\section{Entorno de desarrollo reproducible}
\label{sec:entorno}

% TODO: redactar. Insumo: docs/environment.md.
% - Devcontainer compartido entre máquinas y plataformas (VSCode con
%   Dev Containers, Remote-SSH, Codespaces).
% - El mismo devcontainer.json levanta en local, en servidor remoto y
%   en la nube; el resultado es bit a bit equivalente.
% - uv como gestor de dependencias y de entornos virtuales (binario
%   Rust, configuración unificada en pyproject.toml).
% - Imagen de desarrollo (Microsoft Python + features) distinta de la
%   de producción (multi-stage minimal).
% - Mecanismo recterm para que la sesión asistida pueda observar el
%   terminal del desarrollador durante la depuración.

\section{Disciplina de calidad}
\label{sec:calidad}

% TODO: redactar. Insumo: docs/technologies.md secciones 2 y 5, +
% workflows.
% - TDD desde el primer commit del código.
% - Ruff como linter + formateador (sustituye flake8 + isort + black).
% - mypy en modo estricto sobre src/univorch/.
% - pytest + pytest-cov + Hypothesis (property-based para el resolver,
%   función pura).
% - Cobertura objetivo y alcanzada al cierre del PoC (>=95% en lógica
%   de dominio, 244 tests verdes al cierre del Sprint 3).
% - Tabla con tests por tipo (unidad, integración, property) y módulos.

\section{Integración y entrega continua}
\label{sec:cicd}

% TODO: redactar. Insumo: .github/workflows/ci.yml + publish.yml.
% - CI en cada push y PR a main: ruff + format + mypy + pytest.
% - Publicación a ghcr.io disparada por tags semver: control consciente
%   del usuario sobre cuándo hay versión nueva. Etiquetas vX.Y.Z (fija)
%   y :latest (mutable). Política de versionado: 0.x mientras la API
%   sea inestable.
% - Imagen pública en el registro de paquetes de GitHub, descargable sin
%   login. El profesor solo necesita Docker.

\section{Desarrollo asistido por IA}
\label{sec:ia}

% TODO: redactar. Esta sección es interesante para defender y es
% material original. Ideas:
% - Asistente: Claude (Anthropic). Modelo Opus o Sonnet según fase
%   (decisiones críticas vs trabajo iterativo).
% - Mecanismo de contexto persistente: ficheros claude/ versionados en
%   el repositorio. CLAUDE.md como punto de entrada con imports.
% - Diario (cronológico), decisiones técnicas (DEC-001 a DEC-036),
%   banco de ideas pendientes, contexto de desarrollo: separación de
%   intereses para que cualquier nueva sesión recupere el contexto.
% - Patrón de trabajo: explicar -> aclarar -> confirmar -> ejecutar. El
%   alumno mantiene el control y entiende cada decisión.
% - Distinción: el código lo escribe el asistente, las decisiones las
%   toma el alumno, y la responsabilidad de defender la memoria es del
%   alumno.

\section{Trazabilidad y gestión del conocimiento}
\label{sec:trazabilidad}

% TODO: redactar. Insumo: claude/decisiones.md, claude/diario.md,
% claude/ideas-pendientes.md.
% - Diario cronológico con todas las decisiones tomadas en su contexto
%   temporal.
% - Banco de decisiones técnicas numeradas (DEC-001 a DEC-036) con
%   trazabilidad mutua y referencia al diario.
% - Banco de ideas pendientes, separado por área: futuro inmediato,
%   web GUI, capa docente, memoria del TFG, UX...
% - Vocabulario controlado en docs/glossary.md, fuente única para el
%   glosario de la memoria y del futuro manual del administrador.
